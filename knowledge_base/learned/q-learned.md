# Q Learned Exceptions and Confirmations

**Owner**: q-memory  
**Last Updated**: 2026-04-12  
**Updated By**: (initial placeholder — q-memory writes all entries)

> This file grows with every user interaction. q-memory is the sole agent authorized
> to write here. Both Copilot Chat mode (q-memory Edit tool) and silent hook mode
> (q-learn.py script) append to this file.
>
> Q reads this file on every judgment to avoid re-flagging patterns the user has
> already adjudicated.

---

## Confirmed Wrong (Q-ACCEPT)

> Patterns the user confirmed as violations. Q uses these to reinforce its judgment
> and to calibrate future verdicts in similar contexts.

_No entries yet. Added by q-memory after user responds [Q-ACCEPT]._

---

## Accepted Exceptions (Q-OVERRIDE)

> Patterns the user dismissed with an explanation. Q will not re-flag these patterns
> in matching contexts.

_No entries yet. Added by q-memory after user responds [Q-OVERRIDE: reason]._

---

## Patterns Detected (q-memory Synthesis)

> q-memory adds synthesized patterns here after every 10 verdicts, or when a recurring
> theme is detected across multiple overrides.

_No patterns yet. q-memory updates this section during cross-verdict synthesis._
